# Core dependencies
setuptools>=65.0.0
wheel>=0.38.0

# Monitor control
monitorcontrol>=4.1.0  # VCP/DDC-CI for compatible monitors

# GUI and image processing
pillow>=10.0.0  # Image handling for GUI

# SmartThings API and local WebSocket control
requests>=2.31.0  # HTTP client for Samsung SmartThings cloud API
samsungtvws>=2.6.0  # Local WebSocket control for Samsung monitors (offline mode)
websocket-client>=1.6.0  # WebSocket client library

# Build and packaging
pyinstaller>=6.0.0  # Create standalone executables